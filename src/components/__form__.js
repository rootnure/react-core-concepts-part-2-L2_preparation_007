/**
 * 1. sue form tag and onSubmit handler with event (e)
 * 1.5. Form e access e.target.[name of the input field].value
 * 
 * 2. controlled element: use individual field state for each input field
 * 
 * 3. controlled element: use a single state (object) to handle all the input fields value
 * 
 * 4. uncontrolled element: useRef() hook to create reference to the element and access value by using like: nameRef.current.value
 */